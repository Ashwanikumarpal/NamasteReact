const heading = React.createElement(
  "h1",
  { id: "heading", id: "ac" },
  "hello World from the Rant"
);

console.log(heading);

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(heading);
